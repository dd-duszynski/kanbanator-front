import React from 'react';
import { Layout } from '../../Components';

const Settings: React.FC = () => {
	return (
		// <Layout>
		<section>
			<article>
				<header>
					<h1>Settings</h1>
				</header>
			</article>
		</section>
		// </Layout>
	);
};

export default Settings;
